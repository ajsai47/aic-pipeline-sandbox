# aic-pipeline-sandbox

Throwaway repo to validate AJ's personal `aic-*` SDLC pipeline end to end
(`/aic-build → /aic-pr → /aic-review → /aic-merge`) without touching the real
AI Collective repos. Default integration branch is `staging`, mirroring platform/website.

Safe to delete once the pipeline is validated.
